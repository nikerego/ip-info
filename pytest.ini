[pytest]
log_cli=true
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
addopts = -v
          --cov-config=.coveragerc
          --cov=.
testpaths = tests
pythonpath = .